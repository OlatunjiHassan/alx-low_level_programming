#include <stdio.h>

/**
 * main - this function prints the arguments of printf
 *
 * Return: Always 0
 */
int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,");
  return (0);
}
