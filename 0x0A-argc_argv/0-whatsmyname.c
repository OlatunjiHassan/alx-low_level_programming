#include <stdio.h>
#include <stdlib.h>

/**
 * main - Execution starts here
 *
 * @argc: this  is the number of arguments on the command line
 * @argv: this is the array of all the arguments
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
