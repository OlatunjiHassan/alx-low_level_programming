#include <stdio.h>

/**
 * main - Execution starts from here
 * arguments: void
 *
 * Return: it returns no value so void
 */
int main(void)
{
/**
 * printf - prints the string entered into it
 * @"_putchar": this will be printed by printf and \n is new line
 *
 * Return: its arguments
 */
printf("_putchar");
return (0);
}
