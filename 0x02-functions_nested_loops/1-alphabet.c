#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - prints all the letters of the alphabet
 *
 * Return: Always 0
 */

int print_alphabet(void)
{
	char alp = "a";

	while (al <= 'z')
	{
		putchar(al);
		al++;
	}
	putchar('\n');
}
